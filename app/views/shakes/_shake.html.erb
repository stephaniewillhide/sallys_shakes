<tr>
  <td class="px-3"><%= shake.name %></td>
  <td class="px-3"><%= shake.ingredients.pluck(:name).join(", ") %></td>
  <td class="px-3"><%= shake.calories_per_shake %></td>
  <td class="px-3"><%= link_to("Edit", edit_shake_path(shake)) %></td>
  <td class="px-3"><%= link_to("Delete", shake_path(shake), method: :delete) %></td>
</tr>
